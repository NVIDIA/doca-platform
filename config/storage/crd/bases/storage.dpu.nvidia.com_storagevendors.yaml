---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
    helm.sh/resource-policy: keep
  name: storagevendors.storage.dpu.nvidia.com
spec:
  group: storage.dpu.nvidia.com
  names:
    kind: StorageVendor
    listKind: StorageVendorList
    plural: storagevendors
    singular: storagevendor
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          StorageVendor represents a storage vendor.
          Each storage vendor must have exactly one NVIDIA StorageVendor custom resource object.
          StorageVendor is an internal API, it is not intended to be used by users.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: StorageVendorSpec defines the desired state of StorageVendor
            properties:
              pluginName:
                description: Storage vendor DPU plugin name
                minLength: 1
                type: string
              storageClassName:
                description: Storage vendor class name, deployed on the DPU K8S cluster.
                minLength: 1
                type: string
            required:
            - pluginName
            - storageClassName
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
