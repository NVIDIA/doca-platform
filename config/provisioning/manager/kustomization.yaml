resources:
- manager.yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
images:
- name: controller
  newName: example.com/dpf-system
  newTag: v0.1.0
# PLDM unpack sidecar (same image as BFB registry: python3 + fwpkg_unpack.py + pldmunpackserver).
- name: bfb-registry
  newName: example.com/bfb-registry
  newTag: v0.1.0
