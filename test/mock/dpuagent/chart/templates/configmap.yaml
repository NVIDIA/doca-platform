apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "chart.name" . }}-config
  labels:
  {{- include "chart.labels" . | nindent 4 }}
data:
  config.yaml: |
{{ toYaml .Values.mockConfig | indent 4 }}
