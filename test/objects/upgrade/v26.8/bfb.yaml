apiVersion: provisioning.dpu.nvidia.com/v1alpha1
kind: BFB
metadata:
  name: bf-bundle
  namespace: dpf-operator-system
spec:
  # URL is overridden at runtime by the BFB_IMAGE_URL environment variable.
  # The default below matches the v26.8 repo baseline and is used only when BFB_IMAGE_URL is not set.
  url: https://nbu-nfs.gtm.nvidia.com/auto/sw_mc_soc_release/doca_dpu/doca_3.5.0/last_stable_ubuntu_24.04_64k_pk