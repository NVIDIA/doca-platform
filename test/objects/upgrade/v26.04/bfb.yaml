apiVersion: provisioning.dpu.nvidia.com/v1alpha1
kind: BFB
metadata:
  name: bf-bundle
  namespace: dpf-operator-system
spec:
  # URL is overridden at runtime by the BFB_IMAGE_URL environment variable.
  # The default below matches the current repo baseline and is used only when BFB_IMAGE_URL is not set.
  url: https://content.mellanox.com/BlueField/BFBs/Ubuntu24.04/bf-bundle-3.4.0-92_26.04_ubuntu-24.04_64k_prod.bfb
