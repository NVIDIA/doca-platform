apiVersion: svc.dpu.nvidia.com/v1alpha1
kind: DPUServiceTemplate
metadata:
  name: example
  namespace: dpf-operator-system
spec:
  deploymentServiceName: example
  helmChart:
    source:
      chart: hello-world
      path: ""
      repoURL: https://helm.github.io/examples
      version: 0.1.0
    values:
      image:
        repository: mirror.gcr.io/nginx
        # This should be:
        # - `1.27.5-alpine3.21-slim` for the current version
        # - `1.27.4-alpine3.21-slim` for the old version
        # to test the upgrade path.
        tag: 1.27.5-alpine3.21-slim
