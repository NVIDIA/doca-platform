apiVersion: v1
kind: ConfigMap
metadata:
  name: dpu-cluster-1-audit-policy
  namespace: dpf-operator-system
data:
  audit-policy.yaml: |
    # Log all requests at the Metadata level.
    apiVersion: audit.k8s.io/v1
    kind: Policy
    rules:
    - level: Metadata
