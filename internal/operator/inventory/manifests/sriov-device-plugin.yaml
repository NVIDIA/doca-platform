apiVersion: svc.dpu.nvidia.com/v1alpha1
kind: DPUService
metadata:
  name: sriov-device-plugin
  namespace: dpf-operator-system
spec:
  security:
    privileged: true
  helmChart:
    source:
      repoURL: oci://example.com
      chart: sriov-device-plugin-chart
      version: v0.1.0
