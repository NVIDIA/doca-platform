{{ if .Values.deployHostManifests }}
kind: Deployment
apiVersion: apps/v1
metadata:
  name: {{ include "nvidia-k8s-ipam.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/component: controller
    app.kubernetes.io/part-of: nvidia-k8s-ipam-controller
    dpu.nvidia.com/component: nvidia-k8s-ipam-controller
    {{- include "nvidia-k8s-ipam.labels" . | nindent 4 }}
  annotations:
    kubernetes.io/description: |
      This deployment launches the nv-ipam controller for nv-ipam.
    {{- include "nvidia-k8s-ipam.annotations" . | nindent 4 }}
spec:
  strategy:
    type: RollingUpdate
  replicas: {{ .Values.controllerManager.replicas }}
  # TODO: drop `name: controller` and switch the selector to the DPF-native
  # labels on the next breaking-change release (selector matchLabels is immutable).
  selector:
    matchLabels:
      name: controller
  template:
    metadata:
      labels:
        name: controller
        app.kubernetes.io/component: controller
        app.kubernetes.io/part-of: nvidia-k8s-ipam-controller
        dpu.nvidia.com/component: nvidia-k8s-ipam-controller
        {{- include "nvidia-k8s-ipam.labels" . | nindent 8 }}
      annotations:
        {{- include "nvidia-k8s-ipam.annotations" . | nindent 8 }}
    spec:
      automountServiceAccountToken: false
      priorityClassName: system-cluster-critical
      serviceAccountName: {{ .Values.serviceAccount.name }}
      imagePullSecrets: {{ .Values.imagePullSecrets | default list | toJson }}
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: node-role.kubernetes.io/master
                    operator: Exists
              - matchExpressions:
                  - key: node-role.kubernetes.io/control-plane
                    operator: Exists
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                topologyKey: kubernetes.io/hostname
                labelSelector:
                  matchLabels:
                    app.kubernetes.io/part-of: nvidia-k8s-ipam-controller
                    dpu.nvidia.com/component: nvidia-k8s-ipam-controller
      tolerations:
        - key: node-role.kubernetes.io/master
          operator: Exists
          effect: NoSchedule
        - key: node-role.kubernetes.io/control-plane
          operator: Exists
          effect: NoSchedule
        - key: nvidia.com/gpu
          operator: Exists
          effect: NoSchedule
      {{- with .Values.volumes }}
      volumes:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: controller
          image: "{{ .Values.nvIpam.image.repository }}:{{ .Values.nvIpam.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.nvIpam.pullPolicy }}
          command: [ "/ipam-controller" ]
          args:
            - --leader-elect=true
            - --leader-elect-namespace=$(POD_NAMESPACE)
            - --ippools-namespace=$(POD_NAMESPACE)
          env:
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            {{- with .Values.env }}
              {{- toYaml . | nindent 12 }}
            {{- end }}
          securityContext:
            allowPrivilegeEscalation: false
            runAsUser: 1000
            capabilities:
              drop:
                - "ALL"
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8081
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8081
            initialDelaySeconds: 5
            periodSeconds: 10
          resources: {{ toYaml .Values.nvIpam.controller.resources | nindent 12 }}
          ports:
            - containerPort: 9443
              name: webhook-server
              protocol: TCP
          {{- with .Values.volumeMounts }}
          volumeMounts:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      securityContext:
        runAsNonRoot: true
{{ end }}
